# section information
section:
  name: Blog Posts
  id: recent-posts
  enable: true
  weight: 5
  showOnNavbar: false #true
  # Can optionally hide the title in sections
  # hideTitle: true

# no additional configuration is required
